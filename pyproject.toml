[project]
name = "pafkit"
version = "0.1.0"
description = "A collection of python scripts for processing and visualizing PAF alignment files"
readme = "README.md"
requires-python = ">=3.12"
authors = [
    {name = "Axel Jensen"}
]
dependencies = [
    "matplotlib==3.10.6",
    "numpy==2.3.3",
]
